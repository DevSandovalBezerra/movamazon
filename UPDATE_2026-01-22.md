## Atualização 2026-01-22

### Contexto
- Correções solicitadas nas etapas de inscrição (modalidades e termos).
- Alinhamento do link de regulamento com a página de programação.
- Correções de renderização das imagens dos kits no admin.
- Sem alteração de layout/CSS (apenas lógica e URLs).

### Alterações principais
- **Termos (inscrição)**: corrigida renderização do HTML dos termos (conteúdo agora renderiza com tags corretamente).
- **Regulamento (inscrição)**: link do regulamento passou a usar o mesmo endpoint da programação:
  - `api/uploads/regulamentos/download.php?file=<arquivo>`
- **Regulamento (download público)**: endpoint ajustado para permitir acesso público quando o arquivo pertence a:
  - evento ativo (`eventos.regulamento_arquivo`) **ou**
  - solicitação aprovada (`solicitacoes_evento.link_regulamento`)
- **Fallback de regulamento (inscrição)**: busca automática em `solicitacoes_evento` quando `eventos.regulamento_arquivo` não estiver preenchido.
- **Modalidades (inscrição)**: correção do `src` das imagens dos kits para resolver caminhos reais e evitar `404`.
- **Kits do Evento (admin)**: correção robusta de URL das imagens, com fallback `.jpeg/.jpg -> .png` quando necessário e placeholder apenas se ambas falharem.

### Arquivos impactados
- `frontend/paginas/inscricao/termos.php`
  - Link do regulamento alinhado à programação.
  - Fallback de regulamento em `solicitacoes_evento`.
  - Renderização HTML dos termos.
- `api/uploads/regulamentos/download.php`
  - Liberação de acesso público quando o arquivo é de evento ativo ou solicitação aprovada.
- `frontend/paginas/inscricao/modalidade.php`
  - Resolução robusta de URL da imagem do kit.
- `frontend/js/kits-evento.js`
  - Resolução de URL de imagens com base dinâmica e fallback de extensão.

### Observações técnicas
- **Banco (brunor90_movamazon.sql)**:
  - `kits_eventos.foto_kit` usa `.jpeg`
  - `kit_templates.foto_kit` usa `.png`
  - Ajustado o JS para tentar `.png` quando vier `.jpeg/.jpg`.
- **Sem alterações de layout**: nenhuma mudança em CSS ou estrutura visual.

### Pendências
- Se algum kit ainda renderizar placeholder, verificar o valor exato de `foto_kit` no banco e o arquivo físico em `frontend/assets/img/kits/`.
